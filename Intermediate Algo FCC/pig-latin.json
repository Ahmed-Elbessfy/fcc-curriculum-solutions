{"index.js":"console.clear();\nfunction translatePigLatin(str) {\n  let proccessStr = str; //creates a copy of the input string to be proccessed to keep the input unchanged\n  let firstVowelIndex = proccessStr.match(/[aeuio]/); // the index of the first vowel letter \n  // return proccessStr.match(/^[aieou]/) !== null ? `${proccessStr}way` : firstVowelIndex !== null ? `${proccessStr.slice(firstVowelIndex)}${proccessStr.slice(0,firstVowelIndex)}ay` : `${proccessStr}ay` ;\n  return firstVowelIndex === null ? `${proccessStr}ay` : firstVowelIndex[\"index\"] == 0 ? `${proccessStr}way` :  `${proccessStr.slice(firstVowelIndex[\"index\"])}${proccessStr.slice(0,firstVowelIndex[\"index\"])}ay`; \n  //check if there are a vowel letter in the string so it returns str + ay at the end \n  //then if this vowel is the first letter so it returns str + way\n  //then the vowel isn't at the bgineing so it return the string sliced from the vowel_letter_index to the end_of_the_string + the string sliced form the biginning_of_the_string to the vowel_letter_index + ay at the end\n\n\n}\n\nconsole.log(translatePigLatin(\"ccccc\"));\n// console.log(\"aliforniacay\");"}