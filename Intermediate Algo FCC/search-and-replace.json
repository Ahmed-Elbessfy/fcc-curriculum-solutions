{"index.js":"console.clear();\nfunction myReplace(str, before, after) {\n  let proccessStr = str;\n  let replaced = before;\n  let newWord = after;\n  let caseCheck = replaced[0].toUpperCase() !== replaced[0]  ? newWord : newWord.replace(newWord.charAt(0) , newWord.charAt(0).toUpperCase());  \n  return str.slice(0,str.indexOf(replaced)) + caseCheck + str.slice(str.indexOf(before)+before.length,) \n}\nconsole.log(myReplace(\"A quick brown fox Jumped over the lazy dog\", \"Jumped\", \"leaped\"));"}